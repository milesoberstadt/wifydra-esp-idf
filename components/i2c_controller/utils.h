#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "types.h"

#define DEV_TYPE_STR_LEN 7
void device_type_str(device_type_t type, char *str);

#define DEV_STATE_STR_LEN 15
void device_state_str(device_state_t state, char *str);
